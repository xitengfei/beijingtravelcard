export default interface Scenic{
    id: string,
    name: string,
    area: string,
    link: string,
    dates: Array<string>,
    periods: Array<string[]>,
    level: string,
    limit: string,
    notice: string,
    price: string
}